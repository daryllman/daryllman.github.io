import 'package:flutter/material.dart';

class TechPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tech Page'),
    );
  }
}
